#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main(){
	int i=1,soma=0,potencia,x;

	scanf("%i",&x);
	
	while (i<=5){
	potencia = pow(x,i);
	soma = soma + potencia;
	i=i+1;
}
printf("Resultado: %i",&soma);
}
